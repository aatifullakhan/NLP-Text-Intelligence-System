# Deploy: connect repo at https://render.com — New Web Service → Docker
services:
  - type: web
    name: medscribe
    runtime: docker
    dockerfilePath: ./Dockerfile
    plan: free
    healthCheckPath: /_stcore/health
